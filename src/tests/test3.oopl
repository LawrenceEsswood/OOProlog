class foo.
	X.
	some_pred(A,B,C).
	some_pred_inherit(A,B,C).
	some_pred_inherit(A,B).
	foo(X).
endclass.

class bar extends foo.
	Y.
	some_other_pred(A,B,C).
	some_pred_inherit(A,B).
endclass.

blah(X, X).